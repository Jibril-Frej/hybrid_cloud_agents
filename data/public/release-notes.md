# Release notes

v4.0 adds retrieval on the public side: the public worker now searches a
public document index and returns the most relevant excerpts, instead of a
fixed acknowledgement message.

v3.0 added retrieval on the private side, allowing the orchestrator to
ground answers in private documents without those documents ever leaving the
private cluster.
